//balanço de parênteses

int parenteses(char s[]){
 int n = strlen(n);
 int *p = malloc(n * sizeof(char));
 for (int z = 0; z <= n; z++){
  switch(s){
   case
  }
 }
 return t == 0;
}

int main(){

 return 0;
}
